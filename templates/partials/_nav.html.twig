<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    {# <a class="navbar-brand" href="#">Navbar</a> #}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="/">Accueil
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">La Carte</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Horaires</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Réserver une table</a>
        </li>
        </ul>
       {% if not app.user %}
			<ul class="navbar-nav ms-auto d-lg-flex align-items-lg-end">
				<li class="nav-item me-2 lg-ms-4">
					<a class="nav-link" href="/connexion">Connexion</a>
				</li>
				<li class="nav-item me-4">
					<a class="nav-link" href="/inscription">Inscription</a>
				</li>
			
			{% endif %}
      </ul>
      <div class="d-flex">
		<ul class="navbar-nav me-auto">
			{% if app.user %}
				<li class="nav-item dropdown" style="margin-right: 5rem;">
					<a class="nav-link dropdown-toggle fs-5 fw-bold" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false" style="color: white;">Dropdown
           {# {{ app.user.fullName }} #}
          </a>
					<div class="dropdown-menu">
						{# {% if 'ROLE_ADMIN' in app.user.roles %} #}
							<a class="dropdown-item  fw-bold" href="/admin">Tableau de bord</a>
						{# {% endif %} #}
						<div class="dropdown-divider"></div>
						{# <a class="dropdown-item fw-bold" href="{{ path('security.logout') }}">Déconnextion</a> #}
										{% endif %}
				</li>
			</div>
		</li>
	</ul>
</div>
</div>
    
    </div>
  </div>
</nav>