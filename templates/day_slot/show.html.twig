{% extends 'base.html.twig' %}

{% block title %}DaySlot{% endblock %}

{% block body %}
    <h1>DaySlot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ daySlot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ daySlot.name }}</td>
            </tr>
            <tr>
                <th>IsAvailable</th>
                <td>{{ daySlot.isAvailable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('day_slot.index') }}">back to list</a>

    <a href="{{ path('app_day_slot_edit', {'id': daySlot.id}) }}">edit</a>

    {{ include('day_slot/_delete_form.html.twig') }}
{% endblock %}
