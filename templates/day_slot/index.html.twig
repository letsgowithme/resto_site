{% extends 'base.html.twig' %}

{% block title %}DaySlot index{% endblock %}

{% block body %}
    <h1>DaySlot index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>IsAvailable</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for day_slot in day_slots %}
            <tr>
                <td>{{ daySlot.id }}</td>
                <td>{{ daySlot.name }}</td>
                <td>{{ daySlot.isAvailable ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_day_slots_show', {'id': daySlot.id}) }}">show</a>
                    <a href="{{ path('app_day_slots_edit', {'id': daySlot.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('day_slots.new') }}">Create new</a>
{% endblock %}
