{% extends 'base.html.twig' %}

{% block title %}DaySlots{% endblock %}

{% block body %}
    <h1>DaySlots</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ day_slot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ day_slot.name }}</td>
            </tr>
            <tr>
                <th>IsAvailable</th>
                <td>{{ day_slot.isAvailable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('day_slots.index') }}">back to list</a>

    <a href="{{ path('app_day_slots_edit', {'id': day_slot.id}) }}">edit</a>

    {{ include('day_slots/_delete_form.html.twig') }}
{% endblock %}
