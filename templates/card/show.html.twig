{% extends 'base.html.twig' %}

{% block title %}Card
{% endblock %}

{% block body %}
	<div class="container">
    <h1>{{ card.name }}</h1>
		<div style="width: 600px;">
			{{ form_row(card.imageFile) }}
		</div>
		<div>{{ card.updatedAt ? card.updatedAt|date('Y-m-d H:i:s') : '' }}</div>

<a href="{{ path('card.edit', {'id': card.id}) }}">Modifier</a>
{{ include('card/_delete_form.html.twig') }}
</div>{% endblock %}
