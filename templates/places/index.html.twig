{% extends 'base.html.twig' %}

{% block title %}Menu index
{% endblock %}

{% block body %}
	<div class="container text-center mt-4 mb-4">
	 {% for message in app.flashes('success') %}
			<div class="alert alert-success">
				{{ message }}
			</div>
		{% endfor %} 
    <h1 class="h5-page mt-4 mb-4" style="color: #b5a10a;">Menu du marché</h1>
		<div class="menu-wrapper">
			{% for place in places %}
				<div class="mt-4" style="min-width: 350px;">
					<div class="card-body text-center">
			                <p class="card-text mt-4 fs-5 menu_conditions" style="color: rgb(35, 28, 28); ;">(
							{{ places.nbTotalPlaces }}
							)</p>
						<p class="card-text mt-4 fs-5 menu_conditions" style="color: rgb(35, 28, 28); ;">(
							{{ places.nbBusyPlaces }}
							)</p>
						<p class="card-text mb-4 mt-3 fs-4 me-2 text-light">{{ places.nbAvailablePlaces }}</p>
            	<p class="card-text mt-4 fs-5 menu_conditions" style="color: rgb(35, 28, 28); ;">(
							{{ places.date }}
							)</p>
							</div>
							</div>
						
								{% endfor %}
						<hr>						
						</div>
							</div>
{% endblock %}