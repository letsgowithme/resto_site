{% extends 'base.html.twig' %}

{% block title %}EveningSlots{% endblock %}

{% block body %}
    <h1>EveningSlots</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evening_slot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evening_slot.name }}</td>
            </tr>
            <tr>
                <th>IsAvailable</th>
                <td>{{ evening_slot.isAvailable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evening_slots_index') }}">back to list</a>

    <a href="{{ path('app_evening_slots_edit', {'id': evening_slot.id}) }}">edit</a>

    {{ include('evening_slots/_delete_form.html.twig') }}
{% endblock %}
